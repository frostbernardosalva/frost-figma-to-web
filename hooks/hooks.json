{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup",
        "hooks": [
          {
            "type": "command",
            "command": "command -v python >/dev/null 2>&1 && python \"${CLAUDE_PLUGIN_ROOT}/bin/check-deps.py\" || echo \"[frost-webflow] Python not found - Stage 0.5 asset conversion cannot run. Install: winget install Python.Python.3.12 (Windows) / brew install python@3.12 (macOS) / sudo apt install python3 python3-pip (Linux), then: pip install --upgrade 'Pillow>=11.3'\""
          }
        ]
      }
    ]
  }
}
