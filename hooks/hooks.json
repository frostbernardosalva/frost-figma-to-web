{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup",
        "hooks": [
          {
            "type": "command",
            "command": "if ! command -v python >/dev/null 2>&1; then echo \"[frost-figma-to-web] Python not found - Stage 0.5 asset conversion cannot run. Install: winget install Python.Python.3.12 (Windows) / brew install python@3.12 (macOS) / sudo apt install python3 python3-pip (Linux), then: pip install --upgrade 'Pillow>=11.3'\"; elif [ ! -f \"${CLAUDE_PLUGIN_ROOT}/bin/check-deps.py\" ]; then echo \"[frost-figma-to-web] cannot find bin/check-deps.py under CLAUDE_PLUGIN_ROOT='${CLAUDE_PLUGIN_ROOT}' - dependency check skipped. Python itself is fine.\"; else python \"${CLAUDE_PLUGIN_ROOT}/bin/check-deps.py\"; fi; exit 0"
          }
        ]
      }
    ]
  }
}
